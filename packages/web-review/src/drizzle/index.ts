// Drizzle ORM entry point: table factories for the review overlay schema
// (threads, comments, pins) so consumers can compose them into their own
// Drizzle schema. Implemented in a later work package (WP8).

export const VERSION = "0.1.0";
